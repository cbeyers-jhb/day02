void ft_print_comb(void);

 
{
    int  i, j, k;
    
    for(i = 1; i <= 3; i++)
	
	    for(j = 1; j <= 3; j++)
	   
		    for(k = 1; k <= 3; k++)
	
			    putchar(i);
				putchar(j);
				putchar(k);
		        return 0;
} 
